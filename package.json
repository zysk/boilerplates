{
  "name": "node-with-mongo-be",
  "version": "1.0.0",
  "description": "This is a nodejs base app with express, logger and mongoose setup. It can be used as a boilerplate for creating other nodejs applications.",
  "main": "app.js",
  "scripts": {
    "start": "cross-env NODE_ENV=prod node ./bin/www",
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vikas M",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
